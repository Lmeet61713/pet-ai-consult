# 宠物问诊 v7.2 回答质量与容量保护

版本：`20260823-quality-capacity-v7.2`

## 目标

在 v7.1 的 32 项功能回归全部通过基础上，修复 5 类回答瑕疵，并解决队列准入、SSE 阶段事件、会话锁和第一阶段并发配置问题。

## 回答质量

- 输出清理改为字段感知，不再递归把任意字段中的固定时长禁食改成同一句话。
- 清理后对结构化列表跨字段去重，避免“少量多次、易消化食物”重复。
- 非眼部病例清理误串入的眼周护理子句，同时保留同一条中的切口观察等相关内容。
- 口腔问题仅在“用户口腔关键词 + RAG oral 分类”同时成立时过滤洗澡/洗护建议；用户主动询问洗澡时不误删。
- 短腹泻回答首句先承接当前症状，再进入护理建议。
- 幼猫疫苗/驱虫问题优先 `V17-CAT-PED-001`，并按查询相关性选择知识事实，不再固定只取卡片前 3 条。
- 问诊 Prompt 升级为 `consult_answer_first v2.4.0`，眼部规则改成条件式，新增主题一致性、首句承接和幼猫日程结构要求。

## 容量与稳定性

- 问诊 Worker 第一阶段目标 `10`，活动任务上限 `12`。
- 文本与视觉 vLLM 的 `max-num-seqs` 拆分为两个变量：文本目标 `12`、视觉保持 `8`。
- 视觉网关第一阶段目标并发 `4`。
- PostgreSQL 准入使用事务级 advisory lock，在同一事务中完成“过期回收 + 三/四态统计 + 任务登记”。
- 活动统计包含 `registered/queued/scheduled/processing`，修复突发请求集体穿透。
- 满载抛出 `QueueBusyError`，HTTP 接口真实返回 `503` 和 `Retry-After: 2`。
- `registered/queued/scheduled/processing` 超过 120 秒自动转为 `timeout`，避免异常任务永久占满槽位。
- PG 连接池显式配置为 `pool_size=20`、`max_overflow=10`、`pool_timeout=10s`。

## SSE 与会话

- 队列 SSE 在任务登记前完成原子准入，因此满载时可以在响应开始前返回真实 503。
- Worker 通过现有 `progress` 钩子把阶段事件写入 Redis Stream；SSE 转发处理、视觉、风险、生成和审核阶段。
- 不启用额外两阶段 Token 生成，避免增加 9B 模型负载。
- 会话锁等待由固定 5 秒改为可配置 20 秒。
- 锁租约至少为 `max(配置值, 总请求超时+15秒)`，默认 95 秒。
- 新增会话锁等待、锁超时、幂等等待和幂等超时日志。

## 图片缓存与视觉输出

- 图片观察结果可缓存，Key 包含图片 SHA256、视觉模型版本、视觉 Prompt 版本、预处理边长、输出长度和规范化 `text_hint` 的 SHA256。
- 原始用户文字不出现在缓存 Key 中，同图不同问题不会串用结果。
- `VISION_MAX_TOKENS` 可配置。代码安全默认保持 512；完成 512/320 图片 A/B 回归后再在环境中设置 320，若截断则回退 384/512。

## 兼容性

- 无需修改外部请求和响应字段。
- `QUEUE_BUSY` 从 HTTP 200 业务错误改为真实 HTTP 503，调用方需按 `retryable=true` 和 `Retry-After` 重试。
- PostgreSQL 仅新增辅助索引，无破坏性表结构变更。
- Redis Stream 和视觉缓存均带 TTL，无需手工迁移或清理。

## 本地验证

- `pytest` 25/25 通过，覆盖 v7/v7.1 原有回答守卫和 v7.2 新增回归。
- 新增验证包含 A1-A5、真实 HTTP 503 + `Retry-After`、四态容量统计、过期回收、Redis Stream 往返及视觉缓存 Key 隔离。
- 所有 v7.2 改动文件通过 Ruff 静态检查与 Python `compileall`。
- Compose YAML 结构可解析；本地无 Docker/GPU 守护进程，因此 32 项端到端回归和并发压测按 `DEPLOY_V7_2.md` 在服务器分阶段执行。
